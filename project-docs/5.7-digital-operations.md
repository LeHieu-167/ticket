# 5.7. Số hóa Quy trình Vận hành và Soát vé (Digital Operations & O2O)

## (i) Đặt vấn đề
Soát vé thủ công chậm và dễ gian lận. Quản lý sự kiện và hình ảnh thủ công tốn kém nhân lực.

## (ii) Giải pháp: E-Ticket (QR), Approval Workflow, Cloudinary

### Chứng minh từ Codebase

**1. E-Ticket & QR Code:** `ticket-be/src/main/java/com/ticket/service/QRCodeService.java`

```java
// Tạo QR Code chứa thông tin định danh vé
public byte[] generateQRCodeImage(String content, int width, int height) {
    QRCodeWriter qrCodeWriter = new QRCodeWriter();
    BitMatrix bitMatrix = qrCodeWriter.encode(content, BarcodeFormat.QR_CODE, ...);
    // ...
}

// Nội dung QR duy nhất
public String buildTicketQRContent(String ticketCode, UUID eventId, UUID orderId) {
    return String.format("TICKET:%s|EVENT:%s|ORDER:%s", ticketCode, eventId, orderId);
}
```

**2. Check-in Logic:** `ticket-be/src/main/java/com/ticket/service/TicketService.java`

```java
public TicketResponse checkInTicket(String ticketCode, UUID staffId) {
    // Chặn dùng lại vé đã check-in
    if (ticket.getStatus() == Ticket.TicketStatus.USED) {
        throw new RuntimeException("Vé đã được sử dụng lúc: " + ticket.getCheckedInAt());
    }
    // Cập nhật trạng thái
    ticket.setStatus(Ticket.TicketStatus.USED);
    ticket.setCheckedInBy(staffId);
}
```

**3. Approval Workflow:** `ticket-be/src/main/java/com/ticket/controller/AdminController.java`

```java
@PutMapping("/{eventId}/approve")
public ResponseEntity<?> approveEvent(...) {
    // Chỉ Admin mới được duyệt sự kiện
    event.setStatus(EventStatus.ACTIVE); // Chuyển từ PENDING -> ACTIVE
    event.setReviewedBy(adminId);
}
```

**4. Cloud Storage:** `ticket-be/src/main/java/com/ticket/service/CloudinaryService.java`

```java
public String uploadImage(MultipartFile file, String folder) {
    // Upload lên CDN Cloudinary
    Map uploadResult = cloudinary.uploader().upload(file.getBytes(), ...);
    return uploadResult.get("secure_url").toString();
}
```

## (iii) Kết quả
Quy trình vận hành O2O khép kín, tự động hóa cao. Soát vé nhanh chóng, chính xác. Quản lý hình ảnh và sự kiện chuyên nghiệp, tối ưu băng thông.
